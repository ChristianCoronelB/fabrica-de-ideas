server {
    listen 80;
    server_name TU_DOMINIO_AQUI www.TU_DOMINIO_AQUI;

    client_max_body_size 25M;

    # Redirect www to non-www
    if ($host = www.TU_DOMINIO_AQUI) {
        return 301 https://TU_DOMINIO_AQUI$request_uri;
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 300s;

        # Buffering
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    # Cache static assets
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3000;
        expires 365d;
        add_header Cache-Control "public, immutable";
    }

    # Cache images and uploads
    location /uploads/ {
        proxy_pass http://127.0.0.1:3000;
        expires 30d;
        add_header Cache-Control "public";
    }
}

# HTTPS server (uncomment after installing SSL with certbot)
# server {
#     listen 443 ssl http2;
#     server_name TU_DOMINIO_AQUI;
#
#     ssl_certificate /etc/letsencrypt/live/TU_DOMINIO_AQUI/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/TU_DOMINIO_AQUI/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     client_max_body_size 25M;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 300s;
#
#         proxy_buffering on;
#         proxy_buffer_size 4k;
#         proxy_buffers 8 4k;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 365d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /uploads/ {
#         proxy_pass http://127.0.0.1:3000;
#         expires 30d;
#         add_header Cache-Control "public";
#     }
# }
#
# # HTTP to HTTPS redirect
# server {
#     listen 80;
#     server_name TU_DOMINIO_AQUI www.TU_DOMINIO_AQUI;
#     return 301 https://TU_DOMINIO_AQUI$request_uri;
# }
