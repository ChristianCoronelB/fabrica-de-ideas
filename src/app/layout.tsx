import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fábrica de Ideas",
  description: "Plataforma de innovación donde las ideas se transforman en realidad. Gestión de proyectos, evaluaciones y seguimiento de emprendimientos.",
  keywords: ["Fábrica de Ideas", "innovación", "emprendimiento", "evaluación", "proyectos"],
  authors: [{ name: "Fábrica de Ideas" }],
  openGraph: {
    title: "Fábrica de Ideas",
    description: "Donde las ideas se transforman en realidad",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster richColors position="top-right" />
        </ThemeProvider>
      </body>
    </html>
  );
}
